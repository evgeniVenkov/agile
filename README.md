# Agile Workspace

Небольшое браузерное приложение для оперативного планирования, оценки и отслеживания пользовательских историй в Agile‑процессе. Включает:

- регистрацию и авторизацию пользователей (логин/пароль);
- Kanban‑доску с историей, оценками в Story Points, задачами и статусами;
- архивирование завершённых историй и удаление ненужных элементов;
- страницу аналитики архива с выбором временного диапазона, агрегированными метриками и «velocity» по дням.

## Технологический стек

- **Frontend**: Vue 3 (Vite, Composition API).
- **Backend**: Node.js + Express.
- **База данных**: MySQL (через `mysql2/promise`).

## Структура проекта

```
.
├── README.md
└── test_vue
    ├── server               # backend API + миграции
    └── vue-project          # frontend SPA
```

## Требования

- Node.js 18+
- npm 9+
- MySQL 8.x (или совместимый)

## Настройка окружения

### 1. Подготовка базы данных

```sql
CREATE DATABASE agile CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

Backend при старте автоматически применяет `schema.sql`, создавая все таблицы и архив. Никаких дополнительных миграций запускать не нужно.

### 2. Backend (`test_vue/server`)

1. Перейти в каталог и установить зависимости:

   ```bash
   cd test_vue/server
   npm install
   npm install dotenv
   ```

2. Создать файл `.env` (расположите рядом с `package.json`) со своими параметрами подключения к БД и портом сервера:

   ```
   PORT=4000
   DB_HOST=localhost
   DB_PORT=3306
   DB_USER=root
   DB_PASSWORD=12345
   DB_NAME=agile
   DB_POOL_LIMIT=10
   ```

   > Все переменные имеют значения по умолчанию (см. `src/config/dbConfig.js`), но явное указание упростит перенос на другие среды.

3. Запустить API:

   - для разработки с авто‑перезапуском:

     ```bash
     npm run dev
     ```

   - для продакшена:

     ```bash
     npm run start
     ```

API стартует на `http://localhost:4000` (или на значении `PORT`) и автоматически проверяет/создаёт структуру базы.

### 3. Frontend (`test_vue/vue-project`)

1. Установить зависимости:

   ```bash
   cd test_vue/vue-project
   npm install
   ```

2. Создать `.env` с адресом API:

   ```
   VITE_API_URL=http://localhost:4000
   ```

3. Запуск режима разработки:

   ```bash
   npm run dev
   ```

   По умолчанию Vite откроет приложение на `http://localhost:5173`.

4. Сборка production‑версии:

   ```bash
   npm run build
   npm run preview   # опционально — локальный предпросмотр
   ```

## Основные сценарии

1. **Регистрация / вход**  
   Создайте учётную запись на экране авторизации, затем выполните вход. Сессия хранится в `localStorage` и используется для подписи историй.

2. **Работа с историями**  
   - Создавайте истории через левую панель, задавайте статус и оценку (SP).  
   - Перемещайте историю по статусам через селектор на карточке.  
   - Добавляйте задачи и отмечайте их выполнение с чекбоксами.  
   - Для завершённых историй доступна кнопка «В архив»; для любых — «Удалить».

3. **Аналитика архива**  
   Переключитесь на вкладку «Аналитика архива», выберите интересующий период (по умолчанию последние 30 дней) и получите:
   - суммарные истории/points/задачи и число активных владельцев;
   - «velocity» по дням (кол-во историй и SP);
   - расшифровку последних архивных историй с задачами.

## Полезные файлы

- `test_vue/server/schema.sql` — описание всех таблиц (пользователи, истории, задачи, архив).
- `test_vue/server/src/app.js` — реализация всех REST‑эндпоинтов.
- `test_vue/vue-project/src/App.vue` — основной SPA, включает Kanban и аналитику.
- `test_vue/vue-project/src/services/api.js` — thin‑клиент для HTTP‑запросов.

## Тестирование

В проекте пока нет автотестов. Для проверки корректности достаточно:

```bash
# Frontend
cd test_vue/vue-project
npm run build

# Backend можно прогнать через curl или Postman
```

## Обратная связь

Если потребуется расширить API, добавить авторизацию по токенам или дополнительные отчёты — дайте знать, архитектура уже предусматривает отдельный слой сервиса и хранение архива в БД. Приятной работы! 🎯


